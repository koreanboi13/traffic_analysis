name: Lint

on: push
# on:
#   push:
#     branches: ["main", "develop"]
#   pull_request:
#     branches: ["main", "develop"]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache: true

      - name: Lint stub (no-op)
        run: echo "Lint step is a stub — skipping."